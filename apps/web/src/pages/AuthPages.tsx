import { FormEvent,useState,type ReactNode } from 'react'; import { api } from '../api'; import type { Me } from '../types';
export function Login({onLogin,onNavigate}:{onLogin:(me:Me)=>void;onNavigate:(path:string)=>void}){const demo=import.meta.env.DEV;const [email,setEmail]=useState(demo?'ada@utdallas.edu':''),[password,setPassword]=useState(demo?'StudyBuddy123!':''),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();try{const me=await api<Me>('/auth/login',{method:'POST',body:JSON.stringify({email,password})});onLogin(me);onNavigate(me.onboardingComplete?'/':'/onboarding');}catch(e){setError((e as Error).message)}}return <AuthCard title="Welcome back" subtitle="Continue building your study network."><form onSubmit={submit}><label>University email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="button">Log in</button></form><p>New here? <a href="/register">Create an account</a></p>{demo&&<p className="hint">Demo: ada@utdallas.edu / StudyBuddy123!</p>}</AuthCard>}
export function Register({onLogin,onNavigate}:{onLogin:(me:Me)=>void;onNavigate:(path:string)=>void}){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[displayName,setDisplayName]=useState(''),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();try{await api<Me>('/auth/register',{method:'POST',body:JSON.stringify({email,password,displayName})});const me=await api<Me>('/auth/login',{method:'POST',body:JSON.stringify({email,password})});onLogin(me);onNavigate('/onboarding');}catch(e){setError((e as Error).message)}}return <AuthCard title="Find your people" subtitle="Create a private student profile in under a minute."><form onSubmit={submit}><label>Display name<input value={displayName} onChange={e=>setDisplayName(e.target.value)} required/></label><label>University email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password <small>10+ characters</small><input type="password" minLength={10} value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="button">Create account</button></form><p>Already registered? <a href="/login">Log in</a></p></AuthCard>}
function AuthCard({title,subtitle,children}:{title:string;subtitle:string;children:ReactNode}){return <main className="auth"><section className="auth-card"><a className="brand" href="/login">Study Buddy <span>Finder</span></a><h1>{title}</h1><p>{subtitle}</p>{children}</section></main>}
