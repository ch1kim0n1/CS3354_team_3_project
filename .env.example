# Copy to .env for Docker Compose. Never commit real production secrets.
MYSQL_DATABASE=study_buddy
MYSQL_USER=study_buddy
MYSQL_PASSWORD=change-me-locally
MYSQL_ROOT_PASSWORD=change-root-me-locally
SPRING_PROFILES_ACTIVE=dev
